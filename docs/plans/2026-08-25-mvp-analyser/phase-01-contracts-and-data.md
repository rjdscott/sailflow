# Phase 01: Contracts + data

- **Status:** 🟢 Completed

## Goal

The type contracts everything codes against, the worker protocol, and all reference data transcribed with provenance, before any solver or UI exists.

## Tasks

- [x] `src/core/types.ts`: BoatDefinition, ControlState{dock,race,down?}, Condition, SolveResult, Tiered, RigState, SailShape [O]
- [x] `src/worker/protocol.ts` PROTOCOL_VERSION=1 request/response unions [O]
- [x] `data/boats/j70.json` from class rules + ORC OD certificate, every number `prov`-tagged [S]
- [x] `data/tuning/north-j70.json`, `data/tuning/quantum-j70.json` transcribed from guides with retrieval dates [S]
- [x] `data/polar/orc-j70.json` from ORC Speed Guide (7 TWS, VMG up/down + 60/90/120 rows) [S]
- [x] `PROVENANCE.md`, `ASSUMPTIONS.md` skeletons [S]
- [x] `src/core/boat/validate.ts` + tests (rejects illegal purchase, missing sail, negative EI) [S]
- [x] ADRs 0003 (core/UI boundary), 0004 (TS now, Rust Epic 3), 0008 (third-party data committed)

## Verification

```bash
make check
pnpm test -- validate
```

## Artifacts

`src/core/types.ts`, `src/worker/protocol.ts`, `data/boats/j70.json`, `data/tuning/*.json`, `data/polar/orc-j70.json`, `PROVENANCE.md`, `ASSUMPTIONS.md`

## Progress log

- 2026-08-25 — types.ts, protocol.ts, boat validator + 13 tests, data/boats/j70.json (class rules **2026** edition + ORC OD test certificate), data/tuning/{north,quantum}-j70.json (North Rev 1015 Southern Spars table, 7 bands; Quantum 2020, 11 bands; Loos PT-2 readings kept), data/polar/orc-j70.json (182 rows, VPP 2011 1.02, issued 2012-04-30), PROVENANCE.md + ASSUMPTIONS.md generated by `scripts/provenance.mjs` (wired into `make docs-check`). ADRs 0003, 0004, 0008. Findings: inhauler and gennaker halyard have no purchase limit in rules F.4.2 (marked assumed); ORC cert disp 811 vs class min dry 812; ORC cert gives RM measured 18.5 kg·m/° (added, use in hydro); polar asym TWS 20 prints 137° at 11.5 kt (planing artifact, kept as printed); guides give rake in inches (54.5"/58") not mm, left raw.
- 2026-08-25 — PR #2 squash-merged. Pages URL confirmed HTTP 200.
